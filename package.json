{
  "name": "lockerroom",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "npm run -w apps/server dev & npm run -w apps/web dev",
    "build": "npm -w apps/server run build && npm -w apps/web run build",
    "start": "npm -w apps/server run start & npm -w apps/web run preview"
  }
}